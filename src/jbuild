(library
 ((name async_rpc_kernel)
  (public_name async_rpc_kernel)
  (preprocess (pps (ppx_jane -dont-check-doc-comments-attachment ppxlib.runner)))
  (libraries (core_kernel async_kernel protocol_version_header))

))


(jbuild_version 1)
