(library
 ((name async_rpc_kernel)
  (inline_tests ((javascript build_and_run)))
  (public_name async_rpc_kernel)
  (libraries (core_kernel async_kernel))
  (preprocess (((pps (JANE_KERNEL)) All)))
  (js_of_ocaml ())))
